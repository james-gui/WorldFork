# WorldFork Redis configuration

# Memory policy
maxmemory 1gb
maxmemory-policy allkeys-lru

# Persistence
appendonly yes
appendfilename "appendonly.aof"

# Snapshotting (keep defaults)
save 900 1
save 300 10
save 60 10000
